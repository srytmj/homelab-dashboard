[Unit]
Description=Homelab Cockpit Out-of-Process Redeploy Daemon
After=docker.service
Requires=docker.service

[Service]
Type=simple
User=root
WorkingDirectory=/root/homelab-dashboard
ExecStart=/root/homelab-redeploy.sh --watch
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
